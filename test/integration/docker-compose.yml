version: "3"
services:
  tests:
    build: .
    volumes:
      - ./:/integration
    links:
      - selenium
  selenium:
    image: selenium/standalone-firefox
    ports:
      - "4444:4444"
    shm_size: 2g
